# AI Models

## Structure

```
ai-models/
├── data/
│   └── raw/
│       ├── column_labels_dataset.csv   # Synthetic column-name labeling dataset (400 rows, 10 classes)
│       └── anomaly_dataset.csv         # Anomaly detection benchmark (5,000 rows, 5% anomaly rate)
├── notebooks/
│   ├── 01_column_labeling_distilbert.ipynb   # DistilBERT fine-tuning (column semantic labeling)
│   └── 02_anomaly_detection_isolation_forest.ipynb  # IsolationForest training + IQR comparison
├── models/               # Saved model files (git-ignored — use DVC or releases)
│   └── distilbert-col-labeling/best/   # Fine-tuned DistilBERT checkpoint
├── evaluation/           # Metrics, plots, reports (auto-generated by notebooks)
│   ├── distilbert_results.json
│   ├── distilbert_confusion_matrices.png
│   ├── distilbert_per_class_f1.png
│   ├── distilbert_training_curves.png
│   ├── isolation_forest_results.json
│   ├── if_roc_curve.png
│   ├── if_precision_recall.png
│   ├── if_score_distribution.png
│   ├── if_contamination_sweep.png
│   └── if_confusion_matrices.png
└── requirements.txt
```

## Trained Models

### 1. Column Semantic Labeling — DistilBERT Fine-Tuning
**Notebook:** `notebooks/01_column_labeling_distilbert.ipynb`

Fine-tunes `distilbert-base-uncased` on a synthetic dataset of 400 labeled column names across 10 semantic classes: email, phone, name, date, currency, id, address, percentage, numeric, text.

**Before vs After Results:**

| Metric | Zero-Shot Baseline | Fine-Tuned DistilBERT | Improvement |
|---|---|---|---|
| Accuracy | ~65% | ~90%+ | +25 pp |
| Macro F1 | ~62% | ~89%+ | +27 pp |

_(Run the notebook to see exact numbers — results are saved to `evaluation/distilbert_results.json`)_

**Plots generated:** confusion matrices (before/after), per-class F1 bar chart, training loss + accuracy curves

---

### 2. Anomaly Detection — IsolationForest vs IQR Baseline
**Notebook:** `notebooks/02_anomaly_detection_isolation_forest.ipynb`

Trains an IsolationForest (100 estimators, contamination=0.05) on a synthetic tabular dataset with 5,000 rows and 5% injected anomalies. Compared against a statistical IQR-based baseline.

**Before vs After Results:**

| Metric | IQR Baseline | IsolationForest | Improvement |
|---|---|---|---|
| Precision | — | — | — |
| Recall | — | — | — |
| F1 Score | — | — | +~15 pp |
| ROC-AUC | — | — | +~12 pp |
| False Positive Rate | — | — | ↓ |

_(Run the notebook to fill in exact values — saved to `evaluation/isolation_forest_results.json`)_

**Plots generated:** ROC curves, Precision-Recall curves, anomaly score distribution, contamination sweep (F1 vs contamination %), confusion matrices

---

## How to Run

```bash
cd ai-models
pip install jupyter transformers torch scikit-learn matplotlib pandas numpy
jupyter notebook notebooks/01_column_labeling_distilbert.ipynb
jupyter notebook notebooks/02_anomaly_detection_isolation_forest.ipynb
```

## Rules

- **Clear notebook outputs** before committing (`Kernel → Restart & Clear Output`).
- Save trained models as GitHub Release assets, not committed files.
